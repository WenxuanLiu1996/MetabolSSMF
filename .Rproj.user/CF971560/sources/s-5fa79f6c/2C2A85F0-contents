% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Functions.R
\name{ssmf}
\alias{ssmf}
\title{ssmf(Simplex-structure matrix factorisation)}
\usage{
ssmf(
  data,
  k,
  H = NULL,
  W = NULL,
  meth = c("dirichlet", "uniform", "kmeans", "nmf"),
  lr = 0.01,
  nruns = 50
)
}
\arguments{
\item{data}{Numeric matrix of data.}

\item{k}{The number of prototpyes/clusters.}

\item{H}{Matrix, user input H matrix to start the algorithm. If input is empty, the function will initialise H matrix automatically.}

\item{W}{Matrix, user input H matrix to start the algorithm. If input is empty, the function will initialise W matrix automatically.}

\item{meth}{Specification of method to initialise the W and H matrix, see 'method' in \code{init()}.}

\item{lr}{Optimisation learning rate.}

\item{nruns}{The maximun times of running the algorithm.}
}
\value{
\code{W} The optimised W matrix, containing the values of prototypes.

\code{H} The optimised H matrix, containing the values of soft clustering memberships.

\code{SSE} The sum of square errors/residuals sum of square.
}
\description{
This function is running ssmf model.
}
\details{
Let \eqn{X \in R^{p \times n}} be the data set consisting of \eqn{p} variables and \eqn{n} samples. Given an integer \eqn{k \ll \text{min}(p,n)}, the rank of factorisation, SSMF finds a matrix \eqn{W_{p \times k}} containing the basis vectors in each column and a matrix \eqn{H_{k \times n}}, whose columns are the mixing weights of the samples, such that
}
\examples{
library(MetabolSSMF)
fit1 <- ssmf(data, k = 4, lr = 0.001, H = H)
fit2 <- ssmf(data, k = 4, H = H)
fit3 <- ssmf(data, k = 4, W = W)
fit4 <- ssmf(data, k = 4, meth = 'dirichlet')
fit5 <- ssmf(data, k = 4)
}
\references{
Abdolali, Maryam & Gillis, Nicolas. (2020). Simplex-Structured Matrix Factorization: Sparsity-based Identifiability and Provably Correct Algorithms. \url{https://doi.org/10.1137/20M1354982}
}
\author{
Wenxuan Liu
}
